name = "shorty-schedule-backend"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (ANTHROPIC_API_KEY, SHARED_SECRET) are NOT set here -- see README.md.
# Never put real secret values in this file; it's committed to git.
