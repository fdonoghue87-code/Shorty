import Foundation

/// Template only -- copy this file to Secrets.swift (which .gitignore excludes from the
/// repo) and fill in your real value there. Secrets.example.swift itself is safe to
/// commit since it never holds a real value.
enum Secrets {
    static let scheduleBackendSharedSecret = "REPLACE_WITH_YOUR_SHARED_SECRET"
}
